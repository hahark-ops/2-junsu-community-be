# 진행내역

최종 업데이트: 2026-02-25 (KST)

이 문서는 사람/AI 모두가 바로 이어서 작업할 수 있도록 현재 상태를 정리한 인수인계 문서입니다.

## 1. 저장소/브랜치

- BE: `/Users/junsu/Desktop/2-junsu-community-be`
- FE: `/Users/junsu/Desktop/2-junsu-community-fe`
- 기본 브랜치: `main`
- 최근 반영 커밋(요약)
  - BE: `0280bc0` 리버스 프록시 단일 실행 구성 및 HTTPS 도메인 스크립트 추가
  - BE: `59bdaa6` 도커 과제용 배포 구성 및 스크립트 추가
  - FE: `88d4919` 업로드 경로 오류 수정 및 백엔드 업로드 폴백 적용

## 2. 현재 구현 상태

### 2.1 백엔드 구조

- FastAPI 기반
- Route / Controller / Model 분리 적용
  - Router: `routers/*.py`
  - Controller: `controllers/*.py`
  - Model: `models/*.py`
- 예외: 파일 업로드 로직은 `routers/file.py`에 로직이 상대적으로 크게 남아 있음

### 2.2 업로드 구조

- FE 업로드 기본 동작:
  1. Presigned URL 요청 (`/v1/files/upload-url`) 시도
  2. 실패 시 BE multipart 업로드(`/v1/files/upload`) 폴백
- BE 업로드 Provider 지원:
  - `local`
  - `s3`
  - `lambda`
- 용량 제한(현재 코드 기준):
  - 프로필 이미지: 15MB
  - 게시글 이미지: 20MB

### 2.3 회원가입 프로필 이미지

- 현재는 회원가입 성공 후 자동 로그인 → 이미지 업로드 → 사용자 프로필 반영까지 수행하도록 구현됨
- 기존 문서와 달리 "회원가입 단계 업로드 제거" 상태가 아님

## 3. Docker/배포 구성

### 3.1 로컬 한 번 실행 구성

- 파일: `docker-compose.yml`
- 구성: `nginx + fe + be + db`
- 목표: `docker compose up` 한 번으로 전체 실행

### 3.2 단일 EC2 리버스 프록시 과제 구성

- 파일: `docker-compose.reverse-proxy.yml`
- 구성: `nginx + fe + be + db`
- 스크립트:
  - HTTP 배포: `scripts/proxy_up_single_ec2.sh`
  - HTTPS 적용: `scripts/enable_https_domain.sh`
- Nginx 라우팅:
  - `/` -> FE
  - `/v1`, `/uploads`, `/docs`, `/openapi.json` -> BE

### 3.3 이미지 빌드/푸시/배포 스크립트

- 이미지 푸시: `scripts/push_images.sh`
- 이미지 기반 배포: `scripts/deploy_with_images.sh`

## 4. Lambda/API Gateway 관련

### 4.1 업로드/분석 Lambda (Terraform, zip)

- 위치:
  - `infra/terraform/lambda/index.js`
  - `infra/terraform/lambda_analytics/index.js`
- 경로:
  - 업로드: `/v1/files/upload`, `/v1/files/upload-url`
  - Athena 확인: `/v1/analytics/health`

### 4.2 BE API Lambda (컨테이너 이미지)

- 파일: `Dockerfile.lambda`, `lambda_handler.py`
- Mangum 사용: `lambda_handler.py`
- 핸들러: `lambda_handler.handler`
- 배포 스크립트: `scripts/deploy_be_lambda_container.sh`

## 5. QA/동작 확인 이력

- 인증/세션:
  - 로그인/로그아웃/`/v1/auth/me` 확인 완료 이력 있음
- 게시글/댓글/좋아요:
  - 생성/수정/삭제/좋아요-취소 동작 확인 이력 있음
- 업로드:
  - 프로필/게시글 업로드 및 S3 URL 반영 확인 이력 있음
- Athena 경로:
  - `scripts/test_athena_via_apigw.sh` 성공 이력 있음

## 6. AWS 리소스/비용 관련 최신 상태

아래는 2026-02-25 기준 점검 결과:

- ElastiCache:
  - `my-elasticache` replication group 삭제 요청 완료
  - 관련 cluster/snapshot 삭제 처리 완료
- EC2:
  - `community-dev-be-ec2`, `community-dev-fe-ec2` 모두 `stopped`
- RDS:
  - `community-dev-mysql` 상태 `stopped`
- 추가 과금 리소스 점검:
  - NAT Gateway 없음
  - Elastic IP 없음
  - ELB 없음
  - ECS Cluster 없음
  - EFS 없음(조회 기준)

주의:
- RDS는 stopped 상태여도 스토리지 비용은 발생 가능
- RDS는 최대 7일 후 자동 재시작될 수 있음

## 7. 남은 과제(우선순위)

### P1

- 과제 8 CI/CD 자동화 미완료
  - EC2 자동배포 파이프라인
  - ECS Fargate + CI/CD
  - Lambda 자동배포 파이프라인

### P2

- 문서 정합성 유지
  - 본 문서 기준으로만 운영
  - 기능/아키텍처 변경 시 즉시 갱신

### P2

- 업로드 도메인/HTTPS 최종 실적용
  - 도메인 구매/DNS 설정
  - `scripts/enable_https_domain.sh` 실 적용

## 8. 빠른 실행 명령 모음

로컬 전체 실행:

```bash
cd /Users/junsu/Desktop/2-junsu-community-be
docker compose up -d --build
```

로컬 전체 종료:

```bash
cd /Users/junsu/Desktop/2-junsu-community-be
docker compose down
```

EC2 단일 리버스 프록시 배포:

```bash
cd /opt/2-junsu-community-be
./scripts/proxy_up_single_ec2.sh
```

HTTPS 적용:

```bash
cd /opt/2-junsu-community-be
DOMAIN=example.com EMAIL=you@example.com ./scripts/enable_https_domain.sh
```

## 9. 참고

- 기존 `AI_HANDOFF.md`는 혼선을 막기 위해 삭제함
- 이후 인수인계 문서는 `진행내역.md` 단일 파일로 유지
