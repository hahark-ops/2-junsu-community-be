# 진행내역

최종 업데이트: 2026-03-03 (KST, 업로드 presigned 인증 강화 배포 검증 + 동일 태그 재배포 자동화 스크립트 추가)

이 문서는 사람/AI 모두가 바로 이어서 작업할 수 있도록 현재 상태를 정리한 인수인계 문서입니다.

## 1. 저장소/브랜치

- BE: `/Users/junsu/Desktop/2-junsu-community-be`
- FE: `/Users/junsu/Desktop/2-junsu-community-fe`
- 기본 브랜치: `main`
- 최근 반영 커밋(요약)
  - BE: `f9c2562` 추가 과제: Portainer HTTPS·프라이빗 레지스트리 로컬 구성 및 QA 반영
  - BE: `5fc232d` 문서: 과제9~11 증빙 및 보고서 초안 최신화
  - BE: `82c3c98` CI/CD: EC2 배포 env 보정 자동화로 DB 기동 안정화
  - FE: `88d4919` 업로드 경로 오류 수정 및 백엔드 업로드 폴백 적용

## 2. 현재 구현 상태

### 2.1 백엔드 구조

- FastAPI 기반
- Route / Controller / Model 분리 적용
  - Router: `routers/*.py`
  - Controller: `controllers/*.py`
  - Model: `models/*.py`
- 예외: 파일 업로드 로직은 `routers/file.py`에 로직이 상대적으로 크게 남아 있음

### 2.1.1 과제 8 전 안정화 반영

- 과제 7 범위를 HTTP-only로 고정
  - 리버스 프록시 compose에서 `443` 포트 매핑 제거
  - 단일 진입은 `80`만 사용
- 회원가입 fail-close 정책 적용
  - 이메일/닉네임 중복확인 서버 검증이 성공(200)하지 않으면 가입 버튼 비활성 유지
  - 중복확인 실패(네트워크/타임아웃/5xx) 시 안내 문구 노출:
    - `* 중복 확인에 실패했습니다. 네트워크 확인 후 새로고침 해주세요.`
- 저장형 XSS 차단 적용
  - 게시글 목록/댓글 렌더링을 `innerHTML` 문자열 주입에서 DOM API + `textContent` 방식으로 전환
- 게시글 상세 응답의 좋아요 목록 개인정보 최소화
  - `likes` 항목에서 `email` 제거, `userId/nickname/createdAt`만 반환
- 회원정보 수정 원자성 보강
  - 닉네임 변경 시 `users/posts/comments` 업데이트를 단일 트랜잭션으로 처리
- 세션 만료(7일 TTL) 도입
  - `sessions.expiresAt` 컬럼 및 인덱스 추가
  - 세션 조회 시 `expiresAt > NOW()` 조건 적용
  - 마이그레이션 파일: `scripts/migrations/20260226_add_session_expiry.sql`
- 추가 개인정보 최소화/권한 강화
  - 게시글 상세 응답에서 `writerEmail` 제거
  - 댓글 응답에서 `writerEmail`, `authorEmail` 제거
  - `GET /v1/users/{user_id}`는 로그인 + 본인 조회만 허용, 응답에서 `email` 제거
- 배포 스크립트에 세션 만료 마이그레이션 자동 연결
  - 공통 스크립트: `scripts/run_migrations.sh`
  - 적용 경로:
    - `scripts/compose_up_local.sh`
    - `scripts/deploy_with_images.sh`
    - `scripts/proxy_up_single_ec2.sh`

### 2.1.2 2026-03-03 보안/운영 이슈 4건 수정

- Presigned URL 발급 경로 인증 강화
  - BE에 인증 필수 엔드포인트 추가: `POST /v1/files/upload-url`
  - BE -> 업로드 Lambda 호출 시 `X-Upload-Internal-Token` 헤더 강제
  - 업로드 Lambda는 내부 토큰 없는 요청에 `401` 반환
  - 직접 API Gateway 호출로 presign URL 발급 불가, 인증된 BE 경유만 허용
- Terraform SSH 예시 안전화
  - `terraform.tfvars.example`의 `admin_cidr`를 샘플 `/32`로 변경
  - `variables.tf` validation으로 `0.0.0.0/0` 차단
- 최소비용 운영 기본값 정렬
  - Terraform `enable_rds` default를 `false`로 변경
- 저장소 위생 정리
  - `.gitignore`에 `uploads/` 추가
  - 기존 추적된 `uploads/*.png` 파일은 git 인덱스에서 제거

### 2.2 업로드 구조

- FE 업로드 기본 동작:
  1. Presigned URL 요청 (`/v1/files/upload-url`) 시도
  2. 실패 시 BE multipart 업로드(`/v1/files/upload`) 폴백
- BE 업로드 Provider 지원:
  - `local`
  - `s3`
  - `lambda`
- 용량 제한(현재 코드 기준):
  - 프로필 이미지: 15MB
  - 게시글 이미지: 20MB

### 2.3 회원가입 프로필 이미지

- 현재는 회원가입 성공 후 자동 로그인 → 이미지 업로드 → 사용자 프로필 반영까지 수행하도록 구현됨
- 기존 문서와 달리 "회원가입 단계 업로드 제거" 상태가 아님

## 3. Docker/배포 구성

### 3.1 로컬 한 번 실행 구성

- 파일: `docker-compose.yml`
- 구성: `nginx + fe + be + db`
- 목표: `docker compose up` 한 번으로 전체 실행
- FE 빌드 기준:
  - FE 저장소 루트 Dockerfile 사용: `/Users/junsu/Desktop/2-junsu-community-fe/Dockerfile`
  - 필요 시 `FE_REPO_PATH` 환경변수로 FE 경로 지정

### 3.2 단일 EC2 리버스 프록시 과제 구성

- 파일: `docker-compose.reverse-proxy.yml`
- 구성: `nginx + fe + be + db`
- 스크립트:
  - HTTP 배포: `scripts/proxy_up_single_ec2.sh`
- Nginx 라우팅:
  - `/` -> FE
  - `/v1`, `/uploads`, `/docs`, `/openapi.json` -> BE

### 3.3 이미지 빌드/푸시/배포 스크립트

- 이미지 푸시: `scripts/push_images.sh`
- 이미지 기반 배포: `scripts/deploy_with_images.sh`
- `scripts/push_images.sh` FE 빌드는 `FE_DIR`(기본값 `../2-junsu-community-fe`) 기준으로 동작

### 3.4 추가 과제(로컬): Portainer HTTPS + Private Registry

- 신규 Compose:
  - `docker-compose.portainer.yml`
  - 서비스: `portainer(9443/HTTPS)`, `registry(5000/basic auth)`
  - 포트 충돌 시 `PORTAINER_HTTPS_PORT`, `REGISTRY_PORT` 환경변수로 포트 변경 가능
- 신규 스크립트:
  - `scripts/setup_portainer_local.sh`
    - `mkcert` 인증서 생성
    - `htpasswd` 생성
    - Portainer/Registry 스택 기동
  - `scripts/build_and_push_local_registry.sh`
    - FE/BE/DB 이미지 빌드 후 `localhost:5000`으로 push
  - `scripts/qa_local_additional.sh`
    - Portainer API/Registry API 체크
    - 앱 스택(`docker-compose.reverse-proxy.yml`) 구동 + 마이그레이션
    - 로컬 스모크(`qa_ec2_smoke.sh`) 연동
- 신규 env 예시:
  - `deploy.portainer.local.env.example`
- 신규 가이드:
  - `docs/PORTAINER_REGISTRY_GUIDE.md`

### 3.5 추가 과제 로컬 실행 결과 (2026-03-03)

- 실행 결과: PASS
- 실제 적용 포트:
  - Portainer: `https://localhost:9444`
  - Registry: `http://localhost:5001`
- 주요 검증 통과:
  - Portainer HTTPS 상태 API 200
  - Registry catalog API 200
  - FE/BE/DB 이미지 빌드 + private registry push 성공
  - `docker-compose.reverse-proxy.yml` 기반 앱 스택 기동 성공
  - `qa_local_additional.sh` 내부 스모크 PASS
- 증빙 경로:
  - `/Users/junsu/Desktop/2-junsu-community-be/evidence/additional`

## 4. Lambda/API Gateway 관련

### 4.1 업로드/분석 Lambda (Terraform, zip)

- 위치:
  - `infra/terraform/lambda/index.js`
  - `infra/terraform/lambda_analytics/index.js`
- 경로:
  - 업로드: `/v1/files/upload`, `/v1/files/upload-url`
  - Athena 확인: `/v1/analytics/health`

### 4.2 BE API Lambda (컨테이너 이미지)

- 파일: `Dockerfile.lambda`, `lambda_handler.py`
- Mangum 사용: `lambda_handler.py`
- 핸들러: `lambda_handler.handler`
- 배포 스크립트: `scripts/deploy_be_lambda_container.sh`
- 현재 AWS 실상태(2026-03-03):
  - API Gateway(`community-dev-be-http-api`, `https://4u5mu4n14j.execute-api.ap-northeast-2.amazonaws.com`)는 존재
  - `GET /`, `GET /docs` 응답은 `200` (정상)
  - `GET /v1/auth/me` 비로그인 응답 `401` 확인
  - Lambda 함수 `community-dev-be-api` 재배포 완료
  - DB 의존 API(`GET /v1/posts`) 응답 `200` 확인
  - Lambda는 VPC 연결 + EC2 MySQL(13306) 연동으로 동작

## 5. QA 실행 결과 (2026-02-26)

### 5.1 1단계 로컬 QA

- 실행:
  - `scripts/compose_up_local.sh`
  - `QA_EMAIL=... QA_PASSWORD=... BASE_URL=http://127.0.0.1 scripts/qa_ec2_smoke.sh`
- 결과:
  - 로그인/게시글/댓글/좋아요/업로드/로그아웃 스모크 PASS
  - `increase_view=false` 경로 PASS
  - 게시글/댓글 API에서 이메일 필드 미노출 PASS
  - `GET /v1/users/{id}` 권한(401/200/403) PASS
  - 세션 마이그레이션 자동 적용 및 `sessions.expiresAt` 인덱스 확인 PASS

### 5.2 2단계 AWS QA

- 수행:
  - 중지 상태 EC2/RDS 재기동 후 최신 코드 pull
  - 서버 내 스택 재기동 후 `qa_ec2_smoke.sh` 실행
  - 동일 보안/권한 검증 항목 재검증
- 결과:
  - 자동 스모크 PASS
  - 수동 핵심 플로우(로그인/게시글/댓글/좋아요/업로드) PASS
  - PII 미노출/권한 정책 PASS

### 5.3 3단계 CI/CD 설계 문서화

- 구현 반영:
  - `.github/workflows/ci.yml`
  - `.github/workflows/deploy-ec2.yml`
  - `.github/workflows/deploy-ecs.yml`
  - `.github/workflows/deploy-lambda.yml`
- 문서:
  - `docs/CICD_PLAN.md`를 설계서에서 실행 가이드 기준으로 갱신
- 추가 반영:
  - `deploy-ec2`를 `ci` 성공 기반 자동 실행(`workflow_run`) + 수동 실행 병행으로 변경
  - `ci`에 로컬 compose 기반 `qa_ec2_smoke.sh` 테스트 게이트 추가

### 5.4 GitHub Actions 실행 이력 (EC2/ECS/Lambda)

- CI 성공 이력(예시)
  - `22476517881`  
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22476517881`
    - SHA: `82c3c981c2119a0143c892375dfba2b4b5dc511a`
- EC2 배포 성공 이력(자동 배포)
  - `22476551001` (`deploy-ec2.yml`, `workflow_run`)
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22476551001`
    - SHA: `82c3c981c2119a0143c892375dfba2b4b5dc511a`
- Lambda 배포 성공 이력(수동 증빙)
  - `22469395475` (`deploy-lambda.yml`, `workflow_dispatch`)
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22469395475`
    - SHA: `8887f3d5e46623ed57d5aa9e60dfefe6850dd787`
- ECS 배포 성공 이력(수동 증빙)
  - `22469758970` (`deploy-ecs.yml`, `workflow_dispatch`)
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22469758970`
    - SHA: `8887f3d5e46623ed57d5aa9e60dfefe6850dd787`
- 최신 자동 배포 성공 이력(main push)
  - `22544670598` (`ci.yml`, `push`)
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22544670598`
    - SHA: `bd75f08f67f6f43cc97f280f01d8906d0dde4443`
  - `22544690983` (`deploy-ec2.yml`, `workflow_run`)
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22544690983`
    - SHA: `bd75f08f67f6f43cc97f280f01d8906d0dde4443`

### 5.6 과제 10/11 검증 기록 (2026-03-01)

- 과제 10 (테스트코드 -> CI 게이트)
  - CI 실패 증빙:
    - URL: `https://github.com/hahark-ops/2-junsu-community-be/actions/runs/22544406601`
    - SHA: `9d03e4f...` (의도적 실패 커밋)
  - 게이트 차단 확인:
    - 실패 SHA 기준 `deploy-ec2` 실행 이력 없음(자동 배포 미트리거)
- 과제 11 (GitHub Actions -> 본인 EC2 자동배포)
  - push -> ci -> deploy-ec2 자동 사이클 성공:
    - CI: `22544670598`
    - Deploy: `22544690983`
  - EC2 내부 헬스 확인:
    - SSM 실행 결과: `curl http://127.0.0.1/` -> `HTTP/1.1 200 OK`

메모:
- ECS/Lambda는 과제 증빙용 1회 성공 이력을 남긴 상태이며, 상시 운영 경로는 EC2 단일 배포로 유지합니다.

### 5.7 2026-03-03 보안 수정 배포 검증

- EC2 배포 반영
  - BE 이미지: `150911259323.dkr.ecr.ap-northeast-2.amazonaws.com/community-be:securefix-20260303-1`
  - 배포 대상 인스턴스: `i-065144f4afa152f45`
- 검증 결과
  - `GET /` -> `200`
  - 비로그인 `POST /v1/files/upload-url` -> `401`
  - 로그인 후 `POST /v1/files/upload-url` -> `200` (`uploadUrl/fileUrl` 반환)
  - API Gateway 직접 호출(`X-Upload-Internal-Token` 없음) -> `401`
- 동일 태그 재배포 자동화 스크립트 추가
  - `/Users/junsu/Desktop/2-junsu-community-be/scripts/redeploy_same_tag_ec2.sh`

### 5.5 과제 9 실행 증빙 (2026-03-01)

- 목적:
  - 프로젝트 인프라를 Terraform 기준으로 `destroy -> apply` 재구축하고 증빙 파일을 남김
- 로컬 증빙 경로(커밋 제외):
  - `/Users/junsu/Desktop/2-junsu-community-be/evidence/assignment9-20260301-204358`
- 핵심 파일:
  - `11-destroy-final.txt`: 최종 destroy 성공 로그
  - `12-state-after-destroy.txt`: destroy 후 state 비어있음
  - `20-apply-final.txt`: apply 성공 로그
  - `21-output-after.txt`: 재구축 output
  - `22-state-after-apply.txt`: apply 후 state 목록
  - `23-resource-status-after-apply.txt`: EC2/ALB/Lambda/APIGW 상태
- apply 재생성 핵심 값:
  - `be_public_ip=52.79.70.58`
  - `fe_public_ip=15.164.67.88`
  - `alb_dns_name=community-dev-alb-881195424.ap-northeast-2.elb.amazonaws.com`
  - `upload_api_base_url=https://6g2d13mjlb.execute-api.ap-northeast-2.amazonaws.com`
  - `vpc_id=vpc-0a94a14a1e1fd35b4`

## 6. AWS 리소스/비용 최신 상태

### 6.1 비용 통제 정책 (확정)

- QA 종료 후 `destroy`가 아니라 `stop` 우선 적용
- 장기 미사용/비용 급증 시에만 별도 승인 후 `destroy`
- Terraform에서 `enable_rds=false`로 RDS 생성을 끌 수 있도록 변경
  - 단일 EC2 Docker(db 컨테이너) 실습 시 비용/생성 지연을 줄일 때 사용
  - ECS/Lambda에서 DB 접속이 필요하면 `enable_rds=true` 또는 `db_host_override`를 명시해야 함
- Terraform 기본 템플릿(`terraform.tfvars.example`)도 `enable_rds=false`, `enable_ecs=false` 중심으로 운영 기준 정렬

### 6.2 현재 상태 (2026-03-03 실시간 조회 기준)

- EC2:
  - `community-dev-be-ec2` running (`i-065144f4afa152f45`)
  - `community-dev-fe-ec2` stopped (`i-03b2d60bda8fdcfaa`)
- RDS:
  - 미생성 (`enable_rds=false`)
- ALB:
  - 없음(조회 결과 없음)
- EIP/NAT:
  - 없음(조회 결과 없음)
- Lambda:
  - `community-dev-upload-handler`, `community-dev-analytics-handler`, `community-dev-be-api` active
- API Gateway:
  - `community-dev-upload-api` active (`/v1/analytics/health` 200 확인)
  - `community-dev-be-http-api` active (`/`, `/docs` 200 확인)
- ECS:
  - `community-dev-ecs-cluster` 상태 `INACTIVE` (실행 서비스 0)
- EFS:
  - `community-dev-efs` available
- CloudTrail/S3:
  - `community-dev-trail` 활성
  - 버킷: `community-dev-trail-4a6399`, `community-dev-uploads-4a6399`, `community-dev-athena-4a6399`
- ElastiCache:
  - 사용하지 않음(삭제 완료)
- 참고:
  - 과금 최소화를 위해 EC2 stop 우선, 필요 시 CloudTrail/EFS도 과제 제출 후 정리 검토

## 7. 과제 진행 체크리스트 (2026-03-03)

- 과제 1. FE/BE 이미지 만들기: 완료
- 과제 2. Docker Compose 동시 실행: 완료
- 과제 3. 테스트: 완료
- 과제 4. Docker Hub 이미지 푸시: 완료
  - Docker Hub repo 확인:
    - `hahark283/community-be`
    - `hahark283/community-fe`
    - `hahark283/community-db`
  - ECR/로컬 private registry 푸시 증빙도 별도 보유
- 과제 5. EC2에 Compose 배포: 완료
- 과제 6. BE 이미지 -> Lambda 배포: 완료
  - BE Lambda 함수 `community-dev-be-api` 활성
  - API Gateway 경유 기본 스모크(`/`, `/docs`, `/v1/auth/me`) 통과
  - DB 의존 API(`/v1/posts`) 통과
- 과제 7. 단일 EC2 Nginx Reverse Proxy + MySQL Docker: 완료
- 과제 8. EC2/ECS/Lambda CI/CD 배포: 완료(증빙 기반)
- 과제 9. 인프라 제거 후 Terraform 재구축: 완료(증빙 폴더 보유)
- 과제 10. 테스트코드 -> CI 게이트: 완료
- 과제 11. GitHub Actions -> 본인 EC2 자동배포: 완료(증빙 기준)
  - 비용 최소화 모드에서 EC2가 `stopped`면 `deploy-ec2`는 의도적으로 실패하도록 설정
  - 자동배포 성공 조건: (1) EC2 실행 상태, (2) GitHub Secrets `MYSQL_ROOT_PASSWORD_DEV`, `DB_PASSWORD_DEV` 설정 완료
- 과제 12. Jenkins 연동: 미완료
- 추가 과제 1. FE/BE/Nginx/MySQL Docker 배포: 완료(로컬)
- 추가 과제 2. Portainer private registry 구축: 완료(로컬)
- 추가 과제 2-1. Portainer HTTPS 적용: 완료(로컬 mkcert)

## 8. 남은 과제(우선순위)

### P1

- 보고서(노션) 마감본 작성
  - As-Is/To-Be 아키텍처 비교
  - 장애 시나리오, HA 전략, RTO/RPO
  - 과제 8~11 실행 URL/로그 링크 첨부
- GitHub Actions 배포 필수 시크릿 점검
  - `MYSQL_ROOT_PASSWORD_DEV`
  - `DB_PASSWORD_DEV`

### P2

- 과제 12(Jenkins) 설계/구현
  - Jenkins 단독 또는 GitHub Actions + Jenkins 연계 경로 중 택1

## 9. 빠른 실행 명령 모음

로컬 전체 실행:

```bash
cd /Users/junsu/Desktop/2-junsu-community-be
docker compose up -d --build
```

로컬 전체 종료:

```bash
cd /Users/junsu/Desktop/2-junsu-community-be
docker compose down
```

EC2 단일 리버스 프록시 배포:

```bash
cd /opt/2-junsu-community-be
./scripts/proxy_up_single_ec2.sh
```

## 10. 참고

- 기존 `AI_HANDOFF.md`는 혼선을 막기 위해 삭제함
- 이후 인수인계 문서는 `진행내역.md` 단일 파일로 유지
- CI/CD 설계 기준 문서: `docs/CICD_PLAN.md`
